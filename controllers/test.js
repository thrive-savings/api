module.exports = (User, Queue, Nonus, Transfer, request, config) => ({
  async method (ctx) {
    try {
    } catch (e) {
      console.log('Error catched from "test": ')
      console.log(e.error)
    }

    ctx.body = {}
  }
})
